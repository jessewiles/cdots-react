version: 2
jobs:
  test:
    working_directory: ~/repo
    docker:
        - image: circleci/node:11.4
    steps:
      - checkout

      - run:
          command:
            npm install

      - run:
          command:
            make test

workflows:
  version: 2
  # runs on feature branch commits
  test:
    jobs:
      - test
